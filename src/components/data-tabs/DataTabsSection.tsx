import React from 'react';
import { SimplifiedDataTabsSection } from './SimplifiedDataTabsSection';
import { DataTabsSectionProps } from './types';

export const DataTabsSection = (props: DataTabsSectionProps) => {
  return <SimplifiedDataTabsSection {...props} />;
};